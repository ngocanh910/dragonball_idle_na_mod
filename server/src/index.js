// ============================================================
// Server Entry — boot the emulator
// ============================================================
// Kept as a thin entry so existing tools that spawn
// `node server/src/index.js` (tests, scripts) keep working.

require('./server');
